import { styled } from '@material-ui/styles';
import { Fab } from '@material-ui/core';

export const GreenButton = styled(Fab)({
  background: 'linear-gradient(45deg, #0BAB64 30%, #3BB78F 90%)',
  color: 'white'
});
